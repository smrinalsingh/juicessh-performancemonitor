<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="com.sonelli.juicessh.api.v1.permission.READ_CONNECTIONS" />
    <uses-permission android:name="com.sonelli.juicessh.api.v1.permission.OPEN_SESSIONS" />

    <!-- Android 11+ (targetSdk 30+) filters package visibility. Without this the app
         cannot see JuiceSSH: its ContentProvider query returns null (empty connection
         list) and PluginClient.start()'s getPackageInfo() throws, so the client never
         binds. This makes the whole plugin non-functional on modern devices. -->
    <queries>
        <package android:name="com.sonelli.juicessh" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        tools:replace="android:icon,android:label"
        tools:targetApi="35">

        <activity
            android:name=".activities.MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|keyboard|screenLayout|smallestScreenSize"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.AboutActivity"
            android:exported="false"
            android:label="@string/title_activity_about"
            android:parentActivityName=".activities.MainActivity" />

        <activity
            android:name=".activities.SettingsActivity"
            android:exported="false"
            android:label="@string/settings"
            android:parentActivityName=".activities.MainActivity" />

    </application>

</manifest>
